const Loading = () => {
  return (
    <main className="fixed z-50 top-0 left-0 w-full flex justify-center h-screen items-center bg-slate-100/50">
      <div className="custom-loader"></div>
    </main>
  );
};

export default Loading;
